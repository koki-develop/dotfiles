# https://taskfile.dev

version: '3'

vars:
  GREETING: Hello, World!

tasks:
  pull:
    cmds:
      - git pull origin main
  diff:
    deps: [pull]
    cmds:
      - chezmoi diff
  apply:
    deps: [pull]
    cmds:
      - chezmoi apply -v
